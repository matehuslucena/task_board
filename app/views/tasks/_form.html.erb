<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :priority %><br>
    <%= f.text_field :priority %>
  </div>
  <div class="field">
    <% statuses = options_from_collection_for_select(@status, 'id', 'name') %>
    <%= f.select :status_id,  statuses %><br>
  </div>
  <div class="field">
    <% boards = options_from_collection_for_select(@boards, 'id', 'name') %>
    <%= f.select :board_id,  boards %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
